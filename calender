import datetime

#using now() to get current Time
current_time = datetime.datetime.now()

 # Printing value of now.
print("Time now at greenwich meridian is : ", end = "")
print(current_time)


# printy calendar of year 2025
import calendar
print("\n", calendar.calendar(2025))
